1 -   • Escreva um programa que imprima os números de 1 a 10 usando um loop while.

#include <stdio.h>

 int main(){
 int i=0;    
    
    while(i<=9){
     i++;   
    printf("%d ", i);
    }

    return 0;
}
-------------------------------------------------------------------------

2 -  Crie um programa que solicite um número ao usuário e imprima se ele é positivo, negativo ou zero.

int main(){
int numero;

printf("digite um numero");
scanf("%d", &numero);

if(numero >=1){
printf("é positivo!!");

}else if(numero<=-1){
printf("é negativo!!");

}else{
printf("é zero.");
}
return 0;
}
------------------------------------------------------------------------

3 - Faça um programa que conte de 0 a 20, mas só imprima os números pares.

#include <stdio.h>

int main(){
    int n = 20;
    while(n<=20 && n>=1){
        if(n%2==0){
            printf(" %d", n);
        }
        n = n - 1;
    }
    return 0;
}
-------------------------------------------------------------------------

4 - Escreva um programa que peça ao usuário para adivinhar um número e informe se o palpite está correto.

#include <stdio.h>

 int main(){
     int n=7;
     int i;
     
    printf("adivinhe o numero!!");
    scanf("%d", &i);
    
    if(n==i){
        printf("seu palpite está correto!!");
    }else{
        printf("seu palpite está errado!!");
    }
    return 0;
 }
-----------------------------------------------------------------------------

5 - Crie um programa que imprima os números de 10 a 1 em ordem decrescente.

#include <stdio.h>

int main(){
    int n = 10;
    while(n<=10 && n>=1){
            printf(" %d", n);
        n = n - 1;
}
    return 0;
}
-------------------------------------------------------------------------------

6 -  Faça um programa que calcule a soma dos números de 1 a 50 usando um loop while.

#include <stdio.h>

int main(){
    int n = 1;
    int soma = 0;
    while(n<=50){
        soma = soma + n;
        n++;
    }
    printf("%d é a soma dos números de 1 a 50", soma);
}
--------------------------------------------------------------------------------

7 - Escreva um programa que solicite a idade do usuário e informe se ele é uma criança, adolescente, adulto ou idoso.


